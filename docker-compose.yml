version: "2"
services:
    web:
        image: nginx
        volumes:
            - ./:/usr/share/nginx/html
            - ./.htpasswd:/etc/nginx/.htpasswd
            - ./nginx.cnf:/etc/nginx/conf.d/default.conf
        ports:
            - 8080:80
